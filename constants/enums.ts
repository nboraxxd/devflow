export enum Theme {
  LIGHT = 'light',
  DARK = 'dark',
  SYSTEM = 'system',
}

export enum HeaderHeight {
  Expanded = 88,
  Collapsed = 72,
}

export enum ServiceStatus {
  idle = 'idle',
  pending = 'pending',
  successful = 'successful',
  rejected = 'rejected',
}

export enum QuestionFormType {
  edit = 'edit',
  create = 'create',
}
